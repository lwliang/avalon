spring:
  profiles:
    host: localhost

application:
  multiDb: false

pulsar:
  url: pulsar://${spring.profiles.host}:6650
  enable: false

file:
  file: /data/
  image: ./image/
  video: ./video/
  mode: date