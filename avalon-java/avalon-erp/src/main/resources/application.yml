server:
  port: 8090
  servlet:
    context-path: /erp
spring:
  banner:
    location: banner.txt
  profiles:
    active: dev,erp-dev
  application:
    name: avalon-server
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    encoding: UTF-8
    suffix: .html
    mode: HTML